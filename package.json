{
  "name": "nexo-studio",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "db:generate": "drizzle-kit generate",
    "dev": "node scripts/dev.mjs",
    "check": "node scripts/check.mjs",
    "check:build": "node scripts/check-build.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "drizzle-kit": "0.31.10",
    "drizzle-orm": "0.45.2",
    "wrangler": "^4.131.1"
  },
  "version": "1.0.0",
  "description": "Site da Nexo com formulário de contato persistente em Cloudflare D1",
  "engines": {
    "node": ">=24 <25"
  }
}
